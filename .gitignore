node_modules/
dist/
release/
.vite/
*.log

# Generated dictionary index — rebuilt by `npm run build:dict` from the pinned,
# committed raw source (assets/cedict/cedict_ts.u8) + CedPane cache, so it is
# reproducible offline and need not be committed. (The small hsk-words.json IS
# committed so the service worker's highlight path can load it without a build.)
src/data/cedict.json

# makemeahanzi source files (large; re-downloaded by `npm run convert:chars`).
# The derived assets/char-data/char-data.json IS committed.
assets/char-data/*.txt

# Playwright E2E artifacts
test-results/
playwright-report/

# Personal pre-publish checklist (not part of the repo)
todo.md

# OS
.DS_Store

# NOTE: src/sidepanel/fonts/ + fonts.css ARE committed (vendored Google Fonts,
# generated by `npm run fetch:fonts`) so the build needs no network.
